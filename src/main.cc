#include <stdio.h>
#include <vector>
#include "tree/vanilla_b_plus_tree.h"
#include <limits.h>
#include <algorithm>

using namespace tree;
int main() {
    const int I = 10, J = 15, K = 20;
    int a[I][J][K];
    int b[I][J][K];
}